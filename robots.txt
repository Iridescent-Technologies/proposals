# These pages are public. They are meant to be found.
#
# This file used to allow the crawl while every page refused indexing with a
# meta tag. That was deliberate at the time: the suite was sent to named people,
# and two pages carry qualification specifications that are drafts.
#
# That decision was reversed on 10 September 2026. The suite is now the public
# library, so the crawl is allowed and indexing is allowed with it.
#
# WHAT THAT MEANS, AND IT IS WORTH KNOWING BEFORE ADDING A PAGE HERE. A search
# result shows the title, never the hedge in the body copy. Two pages name a
# qualification specification that has not been submitted to an awarding body,
# and Zavmo is mid-application with awarding bodies who can search. If a new
# page would read badly as a bare title in a search result, give it a title that
# reads correctly on its own, or put the noindex meta tag back on that one page.
#
# _template is excluded below. It is a placeholder deck, not content.

User-agent: *
Allow: /
Disallow: /_template/

Sitemap: https://iridescent-technologies.github.io/proposals/sitemap.xml
